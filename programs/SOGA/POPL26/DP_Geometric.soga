array[10] dataset;

for i in range(10){
    dataset[i] = bern(0.5);
} end for;

count = 0.0;
for i in range(10){
    count = count + dataset[i];
} end for;


/* This the version with noise but we pass it as a string 

array[10] dataset;

for i in range(10){{
    dataset[i] = bern(0.5);
}} end for;

geom1 = {};
geom2 = {};
noise = geom1 - geom2;

count = 0.0;
for i in range(10){{
    count = count + dataset[i];
}} end for;

count = count + noise;

*/
